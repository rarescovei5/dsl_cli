[package]
name = "dsl_cli_core"
description = "Runtime support crate for dsl_cli (argument/option parsing + help/error formatting)."
version = "0.3.0"
categories = ["command-line-interface"]
keywords = ["cli", "argument", "parser"]
readme = "./README.md"
edition = {workspace = true}
license = {workspace = true}
repository = {workspace = true}
